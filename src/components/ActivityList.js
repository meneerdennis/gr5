// Deprecated stub: ActivityList was determined unused. Keeping a minimal
// exported component to avoid breaking imports while removing heavy UI code.
import React from "react";

function ActivityList() {
  // Intentionally render nothing — remove or re-implement if needed.
  return null;
}

export default ActivityList;
